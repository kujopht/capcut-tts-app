# ============================================================
#  Fanfic Audio Studio - phu thuoc
#  Cai dat mot lan:
#    python -m venv .venv
#    .\.venv\Scripts\Activate.ps1
#    pip install -r requirements-gui.txt
# ============================================================

# --- Bat buoc: goi API CapCut ---
requests>=2.25.0

# --- Bat buoc: ung dung desktop (app.py) ---
PySide6>=6.6.0

# --- Bat buoc: doc file .docx ---
python-docx>=1.1.0

# --- Nguon giong Edge TTS (provider "edge") ---
# Pin theo minor de khong bi vo API giua chung; 7.2.8 la ban moi nhat da kiem tra.
edge-tts>=7.2.8,<8.0

# --- Nguon giong Piper local (provider "piper") ---
# Chay hoan toan tren may, model .onnx + .onnx.json.
# LUU Y: ung dung van chay binh thuong khi CHUA cai hai goi duoi day —
# cac giong tuong ung chi bao trang thai "Chưa cài gói" / "Chưa tải model".
piper-tts>=1.6.0,<2.0

# --- Tuy chon: giao dien Gradio du phong (legacy_gradio_app.py / run_gradio.bat) ---
gradio>=4.44

# --- Tuy chon: chi can khi build EXE bang build_app.bat ---
pyinstaller>=6.0
