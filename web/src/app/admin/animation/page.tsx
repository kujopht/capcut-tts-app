"use client";

import { AdminSapXayDung } from "@/components/AdminSapXayDung";

export default function AdminAnimationSeries() {
  return (
    <AdminSapXayDung
      tieuDe="Series Animation"
      moTa="Danh sách/kiểm duyệt series Animation trên toàn nền tảng (mọi chủ sở hữu) — hiện tại tác giả tự quản lý series của mình ở /animation/[id]."
      giaiDoan="giai đoạn Moderation cho Animation"
    />
  );
}
